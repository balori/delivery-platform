apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy-tracker.fullname" . }}
  labels:
    {{- include "deploy-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy-tracker.selectorLabels" . | nindent 4 }}
